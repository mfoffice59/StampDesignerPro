# UI Eraser Logo v3

Исправлено:
- видимые подписи кнопок;
- убрана проблемная серая стилизация;
- ластик получил отдельное состояние рисования;
- холст теперь стабильно принимает мышь;
- добавлен контур ластика;
- добавлена непрозрачность ластика;
- добавлен откат последнего мазка;
- логотип двигается мышью и масштабируется колесом.

Загрузка:
- загрузить `src` поверх текущей версии;
- загрузить `Docs` при желании;
- Commit changes;
- дождаться Actions.
